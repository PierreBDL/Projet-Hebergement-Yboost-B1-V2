# Database Configuration
# Use DATABASE_URL for PostgreSQL (Supabase/Render)
# Or use individual MySQL variables below

# PostgreSQL (Supabase/Render)
DATABASE_URL=postgresql://user:password@host:port/database

# MySQL (fallback)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=bdd_messagerie
DB_USER=root
DB_PASS=

# Server
PORT=3000
